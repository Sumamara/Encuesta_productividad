# Quiz de Habilidades Angulares (Angular Habits Quiz)

Una aplicación interactiva para evaluar 8 pilares clave del bienestar y la productividad personal. Inspirada en los hábitos angulares que sostienen una vida equilibrada.

## 🌟 Características

*   **Evaluación Integral**: 24 preguntas divididas en 8 categorías (Energía, Estrés, Social, Dinero, Metas, Productividad, Confianza, Liderazgo).
*   **Resultados Detallados**:
    *   **Área de Oportunidad**: Identifica tu eslabón más débil con un plan de acción concreto.
    *   **Mayor Fortaleza**: Celebra tu área más fuerte.
    *   **Perfil Completo**: Desglose interactivo de todas las categorías ordenadas por puntuación.
*   **Persistencia de Datos**: Integración con **Supabase** para guardar resultados y tiempos de finalización automáticamente.
*   **Cronómetro**: Mide el tiempo total que toma completar el quiz.
*   **Diseño Responsivo**: Experiencia fluida en móviles y escritorio con animaciones suaves.

## 🛠️ Tecnologías

*   **Frontend**: React, Vite, TypeScript
*   **Estilos**: Tailwind CSS, Shadcn UI, Lucide React
*   **Backend**: Supabase (Base de datos y API)
*   **Animaciones**: Framer Motion / CSS Animations

## 🚀 Cómo correr el proyecto

### 1. Prerrequisitos
Asegúrate de tener Node.js instalado.

### 2. Instalación
```bash
# Clonar el repositorio (si aplica) o navegar a la carpeta
cd Habilidades_angulares

# Instalar dependencias
npm install
```

### 3. Configuración de Supabase
Crea un archivo `.env` en la raíz del proyecto con tus credenciales de Supabase:

```env
VITE_SUPABASE_URL=tu_url_de_supabase
VITE_SUPABASE_ANON_KEY=tu_clave_anonima
```

### 4. Base de Datos (SQL)
Ejecuta el siguiente script en tu SQL Editor de Supabase para crear la tabla necesaria:

```sql
create table public."Habilidades_angulares" (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  user_name text,
  total_score integer,
  time_taken_seconds integer,
  -- Columnas para cada pregunta (q1 a q24)
  q1 integer, q2 integer, q3 integer, q4 integer, q5 integer, q6 integer,
  q7 integer, q8 integer, q9 integer, q10 integer, q11 integer, q12 integer,
  q13 integer, q14 integer, q15 integer, q16 integer, q17 integer, q18 integer,
  q19 integer, q20 integer, q21 integer, q22 integer, q23 integer, q24 integer
);

-- Habilitar seguridad (RLS) y permitir inserciones anónimas
alter table public."Habilidades_angulares" enable row level security;

create policy "Allow anonymous inserts"
on public."Habilidades_angulares"
for insert
to anon
with check (true);
```

### 5. Ejecutar en Desarrollo
```bash
npm run dev
```
Abre [http://localhost:8080](http://localhost:8080) en tu navegador.

## 📂 Estructura del Proyecto

```
src/
├── components/
│   ├── quiz/
│   │   ├── QuizEngine.tsx    # Lógica principal, timer y envío a Supabase
│   │   ├── QuizQuestion.tsx  # Interfaz de pregunta individual
│   │   ├── QuizResult.tsx    # Pantalla de resultados y desglose
│   │   ├── QuizWelcome.tsx   # Pantalla de bienvenida
│   │   └── quizData.ts       # Preguntas, lógica de evaluación y textos
│   └── ui/                   # Componentes reutilizables (Botones, Cards, etc.)
├── integrations/
│   └── supabase/
│       └── client.ts         # Cliente de conexión a Supabase
├── App.tsx                   # Punto de entrada
└── main.tsx
```

## 🎨 Diseño y Personalización

Los colores y estilos están definidos en `index.css` y `tailwind.config.ts`, utilizando un sistema de variables CSS para facilitar cambios de tema.
